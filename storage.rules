rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // ไฟล์ฟอนต์: อ่านได้เฉพาะ authenticated users เท่านั้น
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
